var expect = require("chai").expect;
var mongoose = require('mongoose');
var Promise = require("bluebird");
var searchData = require("../../server/domain/elasticsearch-data.js");
/*


describe("elastic search test", function() {

    it("should able to connect to a known bonsai restful url", function() {
         searchData.testSearch
         .then(function(data){
             searchResult = client.doSearch(client, 'farm');
             done();
         });
    })
    
    
});
*/